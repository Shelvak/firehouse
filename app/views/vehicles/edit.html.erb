<div class="container-fluid">
  <h2><%= show_kind_of_intervention @intervention.kind %> - Vehículo involucrado</h2>
</div>
<%= content_for :form_actions do %>
  <%= link_to t('activerecord.models.intervention.one'), @intervention %> |
  <%= link_to t('activerecord.models.mobile_intervention.one'), [@intervention, @mobile_intervention] %>
<% end %>

<%= render 'form' %>